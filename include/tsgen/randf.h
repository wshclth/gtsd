#ifndef GTSD_INCLUDE_TSGEN_FRANG_H
#define GTSD_INCLUDE_TSGEN_FRANG_H

#define RAND_HALF (RAND_MAX / 2.0)

/* needed for rand() */
#include <stdlib.h>

/*
 * Generates a random double between -1 and 1
 */
double randf();

#endif
